{
  "$schema": "https://turborepo.com/schema.json",
  "globalDependencies": [".env"],
  "ui": "tui",
  "tasks": {
    "sdk#build": {
      "dependsOn": ["db#build", "shared#build"],
      "outputs": ["lib"]
    },
    "db#build": {
      "outputs": ["lib"]
    },
    "shared#build": {
      "dependsOn": ["db#build"],
      "outputs": ["lib"]
    },
    "build": {
      "with": ["db#build"],
      "outputs": [".next/**", "!.next/cache/**", "dist", "lib"]
    },
    "dev": {
      "dependsOn": ["db#build", "shared#build", "sdk#build"],
      "cache": false,
      "persistent": true
    },
    "start": {
      "cache": false,
      "persistent": true
    }
  }
}
