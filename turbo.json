{
  "$schema": "https://turborepo.com/schema.json",
  "globalDependencies": [".env"],
  "ui": "tui",
  "tasks": {
    "db#build": {
      "outputs": ["lib"]
    },
    "shared#build": {
      "dependsOn": ["db#build"],
      "outputs": ["lib"]
    },
    "build": {
      "with": ["db#build", "shared#build"],
      "outputs": [".next/**", "!.next/cache/**", "dist", "lib"]
    },
    "dev": {
      "dependsOn": ["db#build", "shared#build"],
      "cache": false,
      "persistent": true
    }
  }
}
